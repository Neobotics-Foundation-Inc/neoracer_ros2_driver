led_matrix_node:
  ros__parameters:
    # Stable udev symlink to the 8x8 display's USB-UART adapter.
    port: "/dev/osrbot_led_matrix"
    baud: 115200
    input_topic: "/dotmatrix/text"
    append_newline: true
    # Written on startup and shutdown; " " for a blank idle panel.
    idle_text: "N"
