led_matrix_node:
  ros__parameters:
    # Stable udev symlink to the 8x8 display's USB-UART adapter.
    port: "/dev/osrbot_led_matrix"
    baud: 115200
    input_topic: "/led_matrix/command"
    append_newline: true
