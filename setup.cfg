[develop]
script_dir=$base/lib/neoracer_ros2_driver
[install]
install_scripts=$base/lib/neoracer_ros2_driver

[tool:pytest]
markers =
    hardware: tests requiring physical hardware (cameras, IMU, LIDAR, etc.)
filterwarnings =
    ignore:.*multi-threaded.*fork.*:DeprecationWarning
