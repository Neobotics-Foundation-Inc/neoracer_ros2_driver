# ROS2 build artifacts
build/
install/
log/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
.eggs/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# colcon
COLCON_IGNORE
.colcon-defaults.yaml

# rosdep
.rosdep_install_marker

# OS / temp
*.log
*.tmp
*.bak
*.orig

# Jupyter
.ipynb_checkpoints/

# Local-only configuration overrides
neoracer_ros2_driver/config/*.local.yaml

# Model artifacts. .pt weights are committed so a fresh clone can run inference
# without a download. The ONNX is an export intermediate, and an .engine is
# built against one board's GPU and TensorRT; neither is portable or shared.
models/*.onnx
models/*.engine
